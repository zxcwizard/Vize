services:
  postgres:
    image: postgres:latest
    container_name: vchan_pg
    restart: unless-stopped
    environment:
      POSTGRES_USER: root
      POSTGRES_PASSWORD: root
      POSTGRES_DB: vchan
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql
      - ./vchan-api/src/main/resources/sql:/docker-entrypoint-initdb.d

  nats:
    image: nats:latest
    container_name: nats-server
    ports:
      - "4222:4222" # Client port
      - "8222:8222" # HTTP Monitoring port (view in browser at localhost:8222)
      - "6222:6222" # Cluster routing port
    volumes:
      - nats-data:/data
    command: [ "-js", "-m", "8222" ]
    restart: unless-stopped

  vchan-worker:
    build: ./vchan-worker
    expose:
      - "4000"

  vchan-ui:
    build: ./vchan-ui
    volumes:
      - ./vchan-ui:/app
      - /app/node_modules

  vchan-api:
    build: ./vchan-api
    expose:
      - "8080"
    depends_on:
      - postgres

  nginx:
    image: nginx:alpine
    ports:
      - "80:80"
    volumes:
      - ./nginx/vchan.conf:/etc/nginx/conf.d/default.conf:ro
    depends_on:
      - vchan-worker
      - vchan-api
      - vchan-ui

volumes:
  postgres_data:
  nats-data: